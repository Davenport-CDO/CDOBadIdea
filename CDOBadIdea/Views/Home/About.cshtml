@model IEnumerable<string>
@{
    ViewData["Title"] = "About";
}

<section class="section">
    <h2 class="title">Our Members</h2>
    <ul>
        @foreach (var user in Model)
        {
            <li class="subtitle">@user</li>
        }
    </ul>

</section>